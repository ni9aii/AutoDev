# Auto-Dev Fix Plan

> Generated: 2026-07-13T07:48:18
> Total findings: 9
> Critical: 2
> Important: 4
> Minor: 3

## Summary by Reviewer

### architecture Reviewer
- IMPORTANT: 1
- MINOR: 1

### code Reviewer
- CRITICAL: 1
- IMPORTANT: 1
- MINOR: 1

### devops Reviewer
- IMPORTANT: 1
- MINOR: 1

### security Reviewer
- CRITICAL: 1
- IMPORTANT: 1

---

## 🔴 Do Now (Quick Wins)

### Fix 1: SQL injection in db.rs query builder

**Source:** code Reviewer
**Severity:** CRITICAL
**File:** `src/db.rs`
**Line:** 42

**Description:**
User-supplied `name` is concatenated directly into the SQL string
instead of being passed as a bound parameter. An attacker can inject arbitrary
SQL via the query parameter.

**Action:** _To be filled by implementer_

### Fix 2: Hardcoded credentials in example config

**Source:** security Reviewer
**Severity:** CRITICAL
**File:** `examples/app.conf`
**Line:** 7

**Description:**
A plaintext API key is committed in the example config. Even though
it is an example, users copy-paste it verbatim. Load secrets from the environment.

**Action:** _To be filled by implementer_

### Fix 3: `Config` struct mixes concerns

**Source:** architecture Reviewer
**Severity:** IMPORTANT
**File:** `src/config.rs`

**Description:**
The `Config` struct carries both runtime tuning and build-time paths.
Split into `RuntimeConfig` and `Paths` so the watchdog settings can change
without rebuilding the path resolution.

**Action:** _To be filled by implementer_

### Fix 4: Missing input validation on `parse_config`

**Source:** code Reviewer
**Severity:** IMPORTANT
**File:** `src/config.rs`
**Line:** 88

**Description:**
`parse_config` trusts the `timeout` field without range checks.
A negative value silently disables the watchdog.

**Action:** _To be filled by implementer_

### Fix 5: CI caches `target/` but not the cargo git index

**Source:** devops Reviewer
**Severity:** IMPORTANT
**File:** `.github/workflows/ci.yml`
**Line:** 32

**Description:**
The cargo git checkout is uncached, so every CI run re-fetches git
dependencies. Add `~/.cargo/git` to the cache key to cut 30-60s per run.

**Action:** _To be filled by implementer_

### Fix 6: Unbounded request buffer in `net::accept`

**Source:** security Reviewer
**Severity:** IMPORTANT
**File:** `src/net.rs`
**Line:** 130

**Description:**
The read loop appends to a Vec without a size cap. A slowloris-style
client can exhaust memory. Cap at a configurable MAX_REQUEST_BYTES.

**Action:** _To be filled by implementer_

## 🟡 Defer to Next Phase

### Deferred 1: Inconsistent error wrapping

**Source:** architecture Reviewer
**Severity:** MINOR
**File:** `src/error.rs`

**Description:**
Some modules return `anyhow::Error`, others return bespoke enums.
Pick one error strategy at the crate boundary for cleaner call sites.

### Deferred 2: Trailing whitespace in `src/net.rs`

**Source:** code Reviewer
**Severity:** MINOR
**File:** `src/net.rs`

**Description:**
Several lines end with trailing spaces; breaks strict lint gates
in CI.

### Deferred 3: Release workflow has no `draft` review gate

**Source:** devops Reviewer
**Severity:** MINOR
**File:** `.github/workflows/release.yml`

**Description:**
Tags are pushed and released automatically. Add a manual approval
(or `draft: true`) so a bad tag can be caught before it ships.
